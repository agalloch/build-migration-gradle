
moduleName = 'charly'
moduleType = 'framework'

sourceSets {
    main {
        java.srcDir "$parentRoot/fnord/network/service/common/charly"
    }
}

jar {
    destinationDir = file(getInstallRootDir(project))
}

clean.doFirst {
    delete.add(getInstallRootDir(project))
}